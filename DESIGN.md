# 徳本上人 名号碑スタンプラリーアプリ 設計書

**バージョン：** 2.1  
**作成日：** 2026年5月19日  
**更新日：** 2026年6月8日  
**ステータス：** ドラフト

---

## 変更履歴

| バージョン | 更新日 | 更新内容 |
|------|------|------|
| 1.0 | 2026年5月19日 | 初版作成 |
| 1.1 | 2026年5月20日 | 詳細設計追記（RLS・エラーケース等） |
| 1.1.1 | 2026年5月29日 | 画面遷移フロー追記（ブラウザバック・リダイレクト仕様） |
| 2.0 | 2026年6月1日 | 管理ページ（S10）・スポット編集（S11）・スポット申請フォーム（S12）を追加 |
| 2.1 | 2026年6月8日 | 承認待ちバッジ・スポット削除・新規追加ボタンを追加（S10）、スポット新規作成画面を追加（S13）、都道府県入力をドロップダウン選択に変更（S11・S13）、却下済みスポットをスポット管理タブから除外、承認時に申請者へスタンプ自動付与・image_urlsをリセットするバグを修正、S11・S12の地図ピッカーをGoogle Mapsに変更 |

---

# 第1部：基本設計

## 1. システム概要

### 1-1. アプリ概要

江戸時代の僧侶「徳本上人（とくほんしょうにん）」が全国に残した「名号碑（みょうごうひ）」を巡るスマートフォン向け写真スタンプラリーアプリ。

ユーザーは名号碑を実際に訪問し、写真を投稿することでスタンプを獲得する。スタンプ数やエリア制覇状況に応じて称号（バッジ）が自動付与される。

### 1-2. 対象ユーザー

- 主対象：徳本上人・名号碑に興味を持つ歴史・仏教ファン
- 当初：友人・知人への限定公開
- 将来：一般公開予定

### 1-3. データ概要

- 名号碑データ：**66件**（2026年5月時点）
- エリア区分：和歌山県 / 近畿・中部 / 関東・東京 / 東北

---

## 2. 技術スタック

| レイヤー | 技術・サービス | 用途 |
|------|------|------|
| フロントエンド | Next.js 16（App Router）/ TypeScript / Tailwind CSS | 画面描画・ルーティング |
| データベース | Supabase（PostgreSQL + PostGIS） | スポット・スタンプ・称号データ管理 |
| 認証 | Supabase Auth | メール・パスワードによるユーザー認証 |
| ストレージ | Supabase Storage（user-photosバケット） | チェックイン写真の保存・配信 |
| 地図 | Google Maps JavaScript API | スポットのピン表示・地図操作 |
| デプロイ | Vercel | Next.jsのホスティング・CI/CD |
| フォント | Yusei Magic（自己ホスト） | 和風テイストの日本語フォント |

### 2-1. システム構成

```
[ユーザー（スマートフォン）]
        ↓ HTTPS
[Vercel] ← Next.js 16 / TypeScript / Tailwind CSS
        ↓
[Supabase]
  ├── PostgreSQL + PostGIS（データベース）
  ├── Auth（メール・パスワード認証）
  └── Storage（写真保存 / user-photosバケット）
        ↓
[Google Maps JavaScript API]（地図表示）
```

---

## 3. 機能一覧

### 3-1. 認証機能

- メールアドレス／パスワードによる新規登録・ログイン
- ログアウト
- プロフィール管理（ニックネーム・アバター画像）
- アバター画像のトリミング・調整（react-easy-cropによる円形クロップ）

### 3-2. スポット機能

- 名号碑スポット一覧表示（エリアフィルター・キーワード検索）
- スポット詳細表示（名称・所在地・解説・アクセス情報）
- エリア区分：和歌山県 / 近畿・中部 / 関東・東京 / 東北

### 3-3. マップ機能

- Google Maps上にスポットをピン表示
- ピンタップで詳細へ遷移
- ピンチ操作によるズーム・1本指スワイプでパン
- APIキー未設定時はカスタムマップ（フォールバック）表示

### 3-4. チェックイン機能

- スポット詳細ページからチェックイン開始（要ログイン）
- カメラ撮影または写真ライブラリから選択
- 写真をSupabase Storageにアップロード
- スタンプをDBに記録
- GPS位置情報取得（距離チェックは現在無効化中・本番前に有効化予定）

### 3-5. スタンプ・称号機能

- スタンプ帳（獲得写真・碑名・日付を一覧表示）
- 称号（バッジ）自動付与（スタンプ数・エリア制覇などの条件）
- 称号一覧ページ（巡礼の道ビジュアル）
- マイページに獲得済み称号・スタンプ数・達成率を表示

### 3-6. 上人についてページ

- 徳本上人の紹介（人物・木食の行・徳本文字・全国巡礼・参拝マナー）
- イラスト付きカードレイアウト

### 3-7. 管理機能（`is_admin`が`true`のユーザーのみアクセス可）

- 名号碑スポットの一覧表示・編集（名称・所在地・位置情報（Google Mapsをタップして指定）・解説・アクセス情報・場所・エリア。都道府県は47都道府県のドロップダウン選択）
- スポット管理タブで承認待ちのスポットには「承認待ち」バッジを表示
- 名号碑スポットの削除（確認ダイアログあり）
- 名号碑スポットの新規作成（管理者が直接追加・都道府県ドロップダウン選択・`status: approved`で即公開）
- ユーザーからのスポット申請一覧表示
- 申請の承認（承認後すぐ公開・申請者にスタンプ自動付与・申請時の写真をスタンプに紐付け・monuments.image_urlsはリセット）・却下（却下されたスポットはスポット管理タブに表示しない）

### 3-8. ユーザー申請機能（ログイン済みユーザーのみ）

- 未登録の名号碑スポットを新規申請できる
- 入力項目：スポットの名称・現在地（GPS自動取得。GPS取得失敗時または手動変更時はGoogle Mapsをタップして指定）・所在地・都道府県・エリア・解説・アクセス情報・写真
- 申請後は「承認待ち」状態になる
- 管理者が承認後、地図・一覧に公開される
- 承認時に申請者のスタンプが自動付与される（申請時の写真・座標を使用）
- 承認後、他のユーザーもそのスポットにチェックインできるようになる

---

## 4. 画面一覧・画面遷移

### 4-1. 画面一覧

| 画面ID | 画面名 | パス | 認証要否 | 種別 |
|------|------|------|------|------|
| S01 | トップ | / | 不要 | 入口 |
| S02 | ログイン | /login | 不要 | 認証 |
| S03 | 新規登録 | /register | 不要 | 認証 |
| S04 | マップ | /map | 不要 | ボトムナビ |
| S05 | スポット一覧 | /monuments | 不要 | ボトムナビ |
| S06 | スポット詳細 | /monuments/[id] | 不要（チェックインは要） | サブ画面 |
| S07 | 上人について | /about | 不要 | ボトムナビ |
| S08 | マイページ | /mypage | 要 | ボトムナビ |
| S09 | 称号一覧 | /mypage/badges | 要 | サブ画面 |
| S10 | 管理ページ | /admin | 管理者のみ | 管理 |
| S11 | スポット編集 | /admin/monuments/[id]/edit | 管理者のみ | 管理 |
| S12 | スポット申請フォーム | /monuments/submit | 要ログイン | サブ画面 |
| S13 | スポット新規追加 | /admin/monuments/new | 管理者のみ | 管理 |

### 4-2. 画面遷移フロー

```
[S01 トップ]
  ├── はじめる ──────────→ [S02 ログイン]
  │                              ↓ アカウント作成へ
  │                        [S03 新規登録]
  │                         ↓（認証成功・両画面共通）
  └── 地図を見る ─────────→ [S04 マップ] ←──────────┘

ボトムナビ（認証不要）：S04マップ / S05スポット一覧 / S07上人について
ボトムナビ（要ログイン）：S08マイページ

[S04 マップ] ──ピンタップ──→ [S06 スポット詳細]
[S05 スポット一覧] ──選択──→ [S06 スポット詳細]
[S06 スポット詳細] ──チェックイン（要ログイン）──→ チェックインフロー
                                   └→ スタンプ・称号付与 → [S06 スポット詳細]（元の画面に戻る）
[S08 マイページ] ──→ [S09 称号一覧]

【管理者の流れ】
[S08 マイページ]
  └──「管理ページへ」ボタン（管理者のみ表示）──→ [S10 管理ページ]
                                                      ├── スポット管理タブ
                                                      │     ├── 名称検索・エリア・未入力項目フィルター
                                                      │     ├── 承認待ちスポットには「承認待ち」バッジを表示
                                                      │     ├── 「新規追加」ボタン ──→ [S13 スポット新規追加]
                                                      │     │                              └── 保存 → [S10]に戻る
                                                      │     └── スポット選択
                                                      │           └──→ [S11 スポット編集]
                                                      │                 └── 保存 → [S10]に戻る
                                                      └── 承認待ちタブ
                                                            └── 承認 or 却下（S10内で完結）

【ユーザーの申請の流れ】
[S04 マップ] ──「新しいスポットを申請」テキストボタン──→ [S12 スポット申請フォーム]
[S05 スポット一覧] ──「新しいスポットを申請」ボタン──→ [S12 スポット申請フォーム]
[S12] ──送信──→「申請を受け付けました」──→ 元のページ（S04 or S05）に戻る
```

#### ブラウザバック・リダイレクト仕様

| 画面 | 条件 | 動作 |
|------|------|------|
| S01 トップ | ログイン済みでアクセス | サーバーサイドで `/map` に自動リダイレクト（ログイン画面を経由せずマップへ） |
| S02 ログイン | ログイン成功後 | `router.replace('/map')` で遷移。ブラウザ履歴からログインページが除去されるため、戻るボタンでログインページに戻れない |
| S03 新規登録 | 登録成功後 | `router.replace('/map')` で遷移。S02と同様、戻るボタンで登録ページに戻れない |
| S04 マップ | ログイン済みで表示中 | `history.pushState` により、ブラウザの戻るボタンを押してもマップ画面に留まる（他アプリへの意図しない離脱を防止） |
| S04 マップ | 未ログインで表示中 | 戻るボタンの制御なし（通常のブラウザ挙動） |
| S10 管理ページ | 未ログインでアクセス | `/login` にリダイレクト |
| S10 管理ページ | ログイン済みだが管理者ではない | 「このページにはアクセス権限がありません」を表示 → OKで `/map` に戻る |
| S11 スポット編集 | 保存成功後 | S10管理ページに戻る |
| S11 スポット編集 | 未保存の変更ありで戻るボタン | 「変更が保存されていません。破棄して戻りますか？」ダイアログ表示（「編集に戻る」または「破棄して戻る」） |
| S11 スポット編集 | 変更なしで戻るボタン | S10管理ページに戻る |
| S12 スポット申請フォーム | 未ログインでアクセス | `/login` にリダイレクト |
| S12 スポット申請フォーム | 送信成功後 | 元のページ（S04 or S05）に戻る |
| S12 スポット申請フォーム | 入力途中で戻るボタン | 「入力内容が消えます。破棄して戻りますか？」ダイアログ表示（「入力に戻る」または「破棄して戻る」） |
| S12 スポット申請フォーム | 未入力で戻るボタン | 元のページ（S04 or S05）に戻る |
| S13 スポット新規追加 | 未ログインでアクセス | `/login` にリダイレクト |
| S13 スポット新規追加 | ログイン済みだが管理者ではない | `/admin` にリダイレクト |
| S13 スポット新規追加 | 保存成功後 | S10管理ページに戻る |
| S13 スポット新規追加 | 入力途中で戻るボタン | 「入力内容が保存されていません。破棄して戻りますか？」ダイアログ表示（「入力に戻る」または「破棄して戻る」） |
| S13 スポット新規追加 | 未入力で戻るボタン | S10管理ページに戻る |

---

## 5. データベース設計

### 5-1. テーブル一覧

| テーブル名 | 用途 | 種別 |
|------|------|------|
| areas | エリア区分マスタ | マスタ |
| monuments | 名号碑スポット情報 | マスタ |
| profiles | ユーザープロフィール | ユーザー |
| stamps | チェックイン記録 | トランザクション |
| badges | 称号マスタ | マスタ |
| user_badges | ユーザー取得称号 | トランザクション |
| monument_stamp_counts | スポット別チェックイン数（ビュー） | ビュー |
| is_within_checkin_range() | GPS距離チェック関数 | RPC関数 |

### 5-2. カラム定義

#### areas（エリア区分マスタ）

| カラム名 | 型 | 説明 |
|------|------|------|
| id (PK) | integer | エリアID |
| name | text | エリア名（例：和歌山県） |
| slug | text | URL用識別子（例：wakayama） |
| description | text | エリア説明（nullable） |
| sort_order | integer | 表示順 |

#### monuments（名号碑スポット情報）

| カラム名 | 型 | 説明 |
|------|------|------|
| id (PK) | uuid | スポットID |
| name | text | スポット名 |
| location | geometry(Point, 4326) | GPS座標（PostGIS） |
| prefecture | text | 都道府県 |
| address | text | 所在地（nullable） |
| area_id (FK→areas.id) | integer | エリアID（nullable） |
| description | text | スポット解説（nullable） |
| access_info | text | アクセス情報（nullable） |
| image_urls | text[] | 画像URL配列 |
| is_verified | boolean | 管理者承認済みフラグ |
| created_at | timestamp with time zone | 登録日時 |
| status | text | 公開状態（`approved`：公開 / `pending`：承認待ち / `rejected`：却下）。デフォルト：`approved` |
| submitted_by (FK→auth.users.id) | uuid | 申請したユーザーのID（管理者が直接登録した場合はnull） |

#### profiles（ユーザープロフィール）

| カラム名 | 型 | 説明 |
|------|------|------|
| id (PK/FK→auth.users.id) | uuid | ユーザーID |
| nickname | text | ニックネーム |
| avatar_url | text | アバター画像URL（nullable） |
| total_stamps | integer | 累計スタンプ数 ※stamps集計と要整合 |
| is_admin | boolean | 管理者フラグ。デフォルト：`false`。Supabaseダッシュボードから手動で設定 |
| created_at | timestamp with time zone | 登録日時 |
| updated_at | timestamp with time zone | 更新日時 |

> ⚠️ total_stampsはstampsテーブルの集計値と二重管理になるため更新漏れに注意

#### stamps（チェックイン記録）

| カラム名 | 型 | 説明 |
|------|------|------|
| id (PK) | uuid | スタンプID |
| user_id (FK→auth.users.id) | uuid | ユーザーID |
| monument_id (FK→monuments.id) | uuid | スポットID |
| photo_url | text | チェックイン写真URL（Supabase Storage） |
| latitude | float | チェックイン緯度（nullable） |
| longitude | float | チェックイン経度（nullable） |
| checked_in_at | timestamp with time zone | チェックイン日時 |

> 📌 `UNIQUE(user_id, monument_id)` がDB側で設定済み。同一スポットへの複数回チェックインはDBレベルで物理的に防がれている（1ユーザー1碑につき1スタンプ）。

#### badges（称号マスタ）

| カラム名 | 型 | 説明 |
|------|------|------|
| id (PK) | integer | 称号ID |
| name | text | 称号名 |
| description | text | 称号説明 |
| icon_url | text | アイコン画像URL（nullable） |
| condition_type | text | 付与条件種別（stamp_count / area_complete / prefecture_complete） |
| condition_value | jsonb | 付与条件値（例：{"count": 10}） |
| sort_order | integer | 表示順 |

#### user_badges（ユーザー取得称号）

| カラム名 | 型 | 説明 |
|------|------|------|
| user_id (FK→auth.users.id) | uuid | ユーザーID |
| badge_id (FK→badges.id) | integer | 称号ID |
| earned_at | timestamp with time zone | 取得日時 |

> 📌 複合主キー（user_id + badge_id）で同一称号の重複取得を防止

#### monument_stamp_counts（ビュー）

| カラム名 | 型 | 説明 |
|------|------|------|
| monument_id | uuid | スポットID |
| stamp_count | integer | チェックイン総数 |

#### is_within_checkin_range()（RPC関数）

| 引数 | 型 | 説明 |
|------|------|------|
| monument_id | uuid | 対象スポットID |
| user_lat | float | ユーザー現在緯度 |
| user_lng | float | ユーザー現在経度 |
| radius_m | float | チェックイン有効半径（省略可） |

- 戻り値：boolean
- 現在はGPS距離チェック無効化中（本番前に有効化予定）

---

## 6. 外部連携

### 6-1. Google Maps JavaScript API

| 項目 | 内容 |
|------|------|
| 用途 | 名号碑スポットのピン表示・地図操作 |
| 利用機能 | マーカー表示、ピンタップで詳細遷移 |
| フォールバック | APIキー未設定時はカスタムマップを表示 |
| 費用 | 月間リクエスト数に応じた従量課金（要モニタリング） |
| APIキー管理 | 環境変数（.env）で管理・Vercel環境変数に設定 |

### 6-2. Vercel

| 項目 | 内容 |
|------|------|
| 用途 | Next.jsアプリのホスティング・自動デプロイ |
| デプロイ方式 | `vercel --prod` コマンドによる手動デプロイ |
| 費用 | 無料プランあり（個人利用・小規模なら無料枠で対応可） |
| 環境変数 | Supabase URL・APIキー・Google Maps APIキーを設定 |

### 6-3. Supabase

| 項目 | 内容 |
|------|------|
| 用途 | DB・認証・ストレージを一括管理するBaaS |
| DB拡張 | PostGIS（GPS座標・距離計算） |
| Storageバケット | user-photos（チェックイン写真を保存） |
| 費用 | 無料プランあり（ストレージ・DB容量の増加で有料プランへ移行） |
| セキュリティ | RLS（Row Level Security）ポリシーで認証ユーザーのみ書き込み可 |

---

## 7. 非機能要件

| 項目 | 内容 |
|------|------|
| 対応デバイス | スマートフォン（iOS / Android）優先、PCブラウザも対応 |
| セーフエリア対応 | viewport-fit=cover + env(safe-area-inset-*) |
| 認証セキュリティ | Supabase Auth + RLSポリシーによる行レベルセキュリティ |
| ストレージ | Supabase Storage（user-photosバケット、公開設定） |
| GPS | チェックイン時に位置情報取得（距離チェックは本番前に有効化予定） |
| データ件数 | 名号碑データ：66件（2026年5月時点） |

---

## 8. 今後の課題

| # | 内容 | 優先度 |
|------|------|------|
| 1 | GPS距離チェックの本番有効化 | 高 |
| 2 | Google Maps本番用Map ID取得 | 高 |
| 3 | エリア制覇バッジの自動付与実装（area_complete条件が未実装） | 高 |
| 4 | PWAアイコン設定 | 中 |
| 5 | S03新規登録のエラーメッセージ日本語化（現在英語のまま） | 中 |
| 6 | S03登録時のauth・profiles不整合リカバリー処理 | 中 |
| 7 | チェックイン完了時の称号獲得通知表示 | 中 |
| 8 | チェックイン完了後のマイページ・スタンプ帳への誘導 | 中 |
| 9 | データ取得失敗時のエラー表示追加（S04・S05・S08・S09のサイレント失敗改善） | 中 |
| 10 | ユーザーによる名号碑の新規登録機能 | 中 |
| 11 | Storageの孤立ファイルクリーンアップ処理 | 低 |
| 12 | 碑データのさらなる追加（現在66件） | 低 |

---

*本設計書はアプリの実装状況をもとに作成したドラフトです。実装の進捗に合わせて随時更新してください。*

---


---

# 第2部：詳細設計

## 9. RLSポリシー定義

Row Level Security（RLS）とは、テーブルの行単位でアクセス権限を制御する仕組みです。Supabaseはデフォルトでポリシーがない場合は全アクセスを拒否します。

### 9-1. ポリシー一覧

| テーブル | SELECT | INSERT | UPDATE | DELETE | 備考 |
|------|------|------|------|------|------|
| areas | 全員 | 管理者のみ | 管理者のみ | 管理者のみ | エリアマスタ。読み取りは認証不要 |
| monuments | `approved`のみ全員・全件は管理者のみ | ログイン済みユーザー全員（管理者含む） | 管理者のみ | 管理者のみ | 一般ユーザーは`status=approved`のスポットのみ閲覧可。申請（INSERT）はログイン済みなら可。statusは申請者は`pending`、管理者は`approved`をセット |
| badges | 全員 | 管理者のみ | 管理者のみ | 管理者のみ | 称号マスタ。読み取りは認証不要 |
| profiles | 本人のみ | 本人のみ | 本人のみ | 本人のみ | 他ユーザーのプロフィール表示予定なし |
| stamps | 本人のみ | ログイン済みユーザー | 不可 | 不可 | INSERTはサーバーサイド処理のためログイン済みで可 |
| user_badges | 本人のみ | ログイン済みユーザー | 不可 | 不可 | INSERTはサーバーサイド処理のためログイン済みで可 |
| monument_stamp_counts | 全員 | - | - | - | ビュー（読み取り専用）。チェックイン人数を全員に公開 |

> ⚠️ stampsとuser_badgesのUPDATE/DELETEを「不可」にしているのは、チェックイン記録の改ざん防止のため。削除が必要な場合は管理者対応とする。

### 9-2. ポリシー実装方針

- `auth.uid() = user_id` の条件で「本人のみ」を実現する
- 管理者操作はSupabaseダッシュボードから直接行う（管理者用APIは現フェーズでは不要）
- ビュー（monument_stamp_counts）はテーブルのRLSに従うため個別ポリシー不要

---

## 10. インデックス定義

インデックスとは、データベースの検索を高速化するための「目次」です。よく検索する列に設定します。

### 10-1. インデックス一覧

| テーブル | 列名 | インデックス種別 | 目的 |
|------|------|------|------|
| stamps | user_id | Btree | 自分のスタンプ一覧取得（マイページ） |
| stamps | monument_id | Btree | スポット別チェックイン集計 |
| user_badges | user_id | Btree | 自分の称号一覧取得（称号ページ） |
| monuments | area_id | Btree | エリアフィルター検索（スポット一覧） |
| monuments | location | GIST | GPS距離チェック関数（is_within_checkin_range） |

> 📌 現在のデータ量（碑66件・少人数ユーザー）では速度差はほぼ出ません。将来ユーザーが増えた際の改善箇所を明確にするために定義します。

> 📌 locationのGISTインデックスはPostGIS専用の空間索引です。Claude Codeにテーブル作成を指示する際は「locationカラムにGISTインデックスも貼って」と伝えてください。

---

## 11. チェックインの処理フロー

チェックイン機能はこのアプリの核心機能です。複数のステップがあるため、処理の流れを定義します。

### ステップ0：ログイン・チェックイン状態チェック（CheckInButton）

| 状態 | ボタン表示 | タップ時の動作 | 判定方法 |
|------|------|------|------|
| 未ログイン | 「ログインしてチェックインする」 | `/login`へ遷移 | サーバーコンポーネントで確認 |
| ログイン済み・未チェックイン | 「チェックインする」 | モーダルを開く | stampsテーブルを照会 |
| チェックイン済み | 「チェックイン済み」（ボタンなし） | 何もしない | stampsテーブルを照会 |

> 📌 チェックイン完了後は `router.refresh()` でサーバーコンポーネントを再フェッチし、状態を更新する。フロント側でローカルステートは持たない。

### ステップ1：UI操作（CheckInModal）

| # | 操作 | 内容 |
|------|------|------|
| 1 | モーダルを開く | 「チェックインする」をタップしてモーダルが開く |
| 2 | 撮影方法を選ぶ | カメラ撮影 or 写真ライブラリ選択 |
| 3 | プレビュー表示 | 選んだ写真がプレビュー表示される（トリミングなし） |
| 4 | 写真を選び直す | プレビュー右上の × をタップ → 写真選択前の状態に戻る（ファイル入力もリセット） |
| 5 | 送信する | 「スタンプを獲得する」をタップして送信へ |
| 6 | キャンセルする | モーダル右上の × をタップ → モーダルが閉じる |

### ステップ2：位置情報取得（useGeolocation）

| # | タイミング | 内容 |
|------|------|------|
| 1 | モーダルが開いた瞬間 | `useGeolocation()` フックが自動的にGPS取得を開始（バックグラウンド） |
| 2 | 取得中 | モーダル上に「GPS取得中…」インジケーターを表示 |
| 3 | 取得中 | 「スタンプを獲得する」ボタンは非活性（押せない） |
| 4 | 取得完了 | ボタンが活性化されて送信可能になる |
| 5 | 取得失敗 | エラーメッセージを表示・`latitude`が`null`のままのためボタンは非活性のままでチェックイン不可 |

> 📌 「距離チェック無効化中」とはスポットから離れた場所でもチェックインできるという意味であり、GPS自体が取れなくてもよいという意味ではない。GPS取得はチェックインの必須条件。

> 📌 ボタンの非活性条件：`!file`（写真未選択）/ `geoLoading`（GPS取得中）/ `status === 'uploading'`（アップロード中）/ `latitude === null`（GPS未取得）のいずれかに該当する場合。

### ステップ3：距離チェック（現在無効化中）

| # | 内容 |
|------|------|
| 1 | `haversineDistance()` でユーザーの現在地とスポットの距離を計算する実装あり |
| 2 | 現在はコメントアウト中（開発中のため） |
| 3 | 本番前に有効化予定 |
| 4 | チェックイン有効半径は `CHECKIN_RADIUS_M` という定数で管理（現在値：50m） |

> 📌 有効化後は距離が50mを超えている場合、エラーメッセージを表示してチェックイン不可となる。

> ⚠️ 50mはかなり厳しい設定のため、本番運用時に実態に合わせて `CHECKIN_RADIUS_M` の値を調整すること。

### ステップ4：写真アップロード（Supabase Storage）

| # | 内容 |
|------|------|
| 1 | バケット：`user-photos`（公開設定） |
| 2 | 保存パス：`stamps/{user_id}/{monument_id}/{timestamp}.{ext}` |
| 3 | `upsert: false`（同一パスへの上書き不可） |
| 4 | 拡張子は元ファイルの拡張子をそのまま使用（変換処理なし） |

> 📌 保存パスに `user_id` が含まれるため、Storageポリシーも `user_id` ベースで制御可能。

> ⚠️ 拡張子は端末・選択方法によって異なる。iPhoneカメラは `heic`、AndroidやJPEG選択時は `jpg` など。本番運用時に `heic` 形式が表示できない環境がないか確認が必要。

### ステップ5：スタンプ保存（Supabase DB）

| # | 内容 |
|------|------|
| 1 | `stamps` テーブルにINSERT |
| 2 | 保存内容：`user_id` / `monument_id` / `photo_url` / `latitude` / `longitude` |
| 3 | ユニーク制約：`UNIQUE(user_id, monument_id)` がDB側で設定済み（1ユーザー1碑につき1スタンプ） |
| 4 | 重複チェック：エラーコード `23505`（ユニーク制約違反）で「スタンプ済み」を判定 |

> 📌 同一スポットへの複数回チェックインはDBレベルで物理的に防がれている。

### ステップ6：称号の自動付与

| # | 内容 |
|------|------|
| 1 | `stamps` へのINSERT後、DBトリガー `on_stamp_created` が自動実行 |
| 2 | FORループで全称号条件を一括チェック |
| 3 | 条件を満たす称号があれば `user_badges` に自動INSERT |
| 4 | 複数条件を同時に満たした場合は複数の称号が一度に付与される |

#### 称号付与条件一覧

| 種別 | 条件 | 称号名 |
|------|------|------|
| スタンプ数 | 1基 | 旅立ちの巡礼者 |
| スタンプ数 | 10基 | 心清らかな旅人 |
| スタンプ数 | 50基 | 名号を辿る者 |
| スタンプ数 | 100基 | 上人の道を歩む者 |
| スタンプ数 | 200基 | 全国を巡る行者 |
| エリア制覇 | 和歌山県（area_id:1）全碑 | 紀ノ国を巡りし者 |
| エリア制覇 | 関東・東京（area_id:2）全碑 | 江戸を巡りし者 |

> 📌 称号名はDB上の値を正とする。今後変更する場合はSupabase SQL Editorで更新すること。

> ⚠️ 現在のトリガー `on_stamp_created` は `stamp_count` 条件のみ処理している。`area_complete` 条件（紀ノ国を巡りし者・江戸を巡りし者）は自動付与されない未実装の状態。本番前に対応が必要。

### ステップ7：完了

| # | 内容 |
|------|------|
| 1 | 成功時はモーダルを閉じる |
| 2 | `router.refresh()` でページ全体を再読み込み |
| 3 | サーバーコンポーネントが `stamps` テーブルを再確認し、「チェックイン済み」表示に更新 |

> ⚠️ 称号獲得の通知・ポップアップは未実装。チェックイン完了時に獲得称号を表示する機能は今後の改善ポイント。

> ⚠️ 完了後はスポット詳細ページに戻るだけで、マイページ・スタンプ帳への誘導ボタンは未実装。今後の改善ポイント。

---

## 12. エラーケース定義

### 12-1. チェックイン処理（S06）

**ステップ0：ログイン確認**

| # | 条件 | 挙動 |
|------|------|------|
| E-01 | 未ログイン | 「ログインしてチェックインする」ボタンを表示。タップで `/login` へ遷移 |

**ステップ2：位置情報取得**

| # | 条件 | 挙動 |
|------|------|------|
| E-02 | ブラウザがGPS非対応 | 「このブラウザはGPSに対応していません」を表示。ボタンは非活性のまま（チェックイン不可） |
| E-03 | GPS取得失敗（拒否・タイムアウト等） | ブラウザのエラーメッセージを表示。ボタンは非活性のまま（チェックイン不可） |
| E-04 | GPS取得中（loading状態） | 「GPS取得中…」インジケーターを表示。ボタンは非活性 |

**ステップ3：距離チェック（現在無効化中）**

| # | 条件 | 挙動 |
|------|------|------|
| E-05 | スポットから50m超（有効化後） | 「名号碑まで約○m離れています。50m以内に近づいてください。」を表示。チェックイン不可 |

**ステップ4：写真アップロード**

| # | 条件 | 挙動 |
|------|------|------|
| E-06 | Storageアップロード失敗 | 「写真のアップロードに失敗しました」を表示。処理中断 |

**ステップ5：スタンプ保存**

| # | 条件 | 挙動 |
|------|------|------|
| E-07 | 同一スポットに既にスタンプあり（DB制約違反23505） | 「この名号碑はすでにスタンプ済みです」を表示 |
| E-08 | その他のDBエラー | 「チェックインに失敗しました」を表示 |

**UI操作**

| # | 条件 | 挙動 |
|------|------|------|
| E-09 | 写真未選択で送信ボタンを押す | ボタンが非活性のため送信不可（`!file` チェック） |

> 📌 E-06〜E-08はモーダル内に赤背景のエラーメッセージとして表示される。

> 📌 エラー発生後もモーダルは閉じず、ユーザーが再試行できる状態を維持する。

> ⚠️ E-07以外のエラー時、アップロード済み写真がStorageに残る可能性あり（クリーンアップ処理なし）。本番前に対応を検討すること。

### 12-2. S02 ログイン画面

| # | 条件 | 表示メッセージ | 挙動 |
|------|------|------|------|
| E-01 | メールアドレス未入力で送信 | ブラウザ標準メッセージ | `required`属性によりブラウザが制御。送信不可 |
| E-02 | パスワード未入力で送信 | ブラウザ標準メッセージ | `required`属性によりブラウザが制御。送信不可 |
| E-03 | メールアドレスまたはパスワードが間違い | 「メールアドレスまたはパスワードが正しくありません」 | パスワード欄とボタンの間にアンバー背景で表示。再試行可能 |
| E-04 | 存在しないアカウント | 「メールアドレスまたはパスワードが正しくありません」 | E-03と同じ（セキュリティのため存在有無を区別しない） |
| E-05 | 送信中（通信中） | なし（ローディングスピナー表示） | ボタンが非活性（opacity-60）になり二重送信を防止 |

> 📌 E-01・E-02はHTMLの`required`属性によるブラウザ標準バリデーション。実装側での独自メッセージなし。

> 📌 E-03・E-04はSupabaseが返すエラーの種類に関わらず一律同じメッセージを表示（意図的な設計）。

### 12-3. S03 新規登録画面

| # | 条件 | 表示メッセージ | 挙動 |
|------|------|------|------|
| E-01 | ニックネーム未入力で送信 | ブラウザ標準メッセージ | `required`属性によりブラウザが制御。送信不可 |
| E-02 | メールアドレス未入力で送信 | ブラウザ標準メッセージ | `required`属性によりブラウザが制御。送信不可 |
| E-03 | パスワード未入力で送信 | ブラウザ標準メッセージ | `required`属性によりブラウザが制御。送信不可 |
| E-04 | パスワードが8文字未満 | ブラウザ標準メッセージ | `minLength={8}`によりブラウザが制御。送信不可 |
| E-05 | すでに登録済みのメールアドレス | Supabaseのエラーメッセージをそのまま表示（英語） | パスワード欄とボタンの間に赤背景で表示。再試行可能 |
| E-06 | その他のSupabase認証エラー | Supabaseのエラーメッセージをそのまま表示（英語） | 同上 |
| E-07 | 認証成功後のプロフィール作成失敗 | 「プロフィールの作成に失敗しました」 | パスワード欄とボタンの間に赤背景で表示。再試行可能 |
| E-08 | 送信中（通信中） | なし（ローディングスピナー表示） | ボタンが非活性になり二重送信を防止 |

> 📌 ニックネームは`maxLength={20}`で20文字制限あり。超過はブラウザが入力を制限するためエラー表示なし。

> 📌 E-05・E-06はSupabaseのエラーメッセージを英語のまま表示（ログイン画面と異なり日本語変換なし）。本番前に日本語化を検討すること。

> ⚠️ E-07はauthユーザーは作成済みだがprofilesテーブルへのINSERTが失敗した状態。authとprofilesが不整合になる可能性あり。本番前にリカバリー処理を検討すること。

### 12-4. S04 マップ画面

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | Google Maps APIキー未設定 | エラーなし。カスタムマップ（フォールバック）に自動切り替え |
| E-02 | スポットデータの取得失敗 | エラーメッセージなし。ピンが表示されないだけ（サイレント失敗） |
| E-03 | スポットの座標が日本範囲外（緯度31〜42°・経度129〜142°外） | エラーなし。該当ピンのみ表示されない |

> ⚠️ E-02はサイレント失敗のため、ユーザーはデータ取得失敗に気づけない。本番前にエラー表示の追加を検討すること。

### 12-5. S05 スポット一覧画面

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | データ読み込み中 | 「読み込み中...」を表示 |
| E-02 | フィルター・検索で該当なし | 「該当するスポットがありません」を表示 |
| E-03 | データ取得失敗 | エラーメッセージなし。E-02と同じ表示になる（サイレント失敗） |

> ⚠️ E-03はサイレント失敗のため、データ取得失敗とフィルター結果ゼロが区別できない。本番前にエラー表示の追加を検討すること。

### 12-6. S06 スポット詳細画面（チェックイン処理以外）

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | 存在しないスポットID（URLを直打ちなど） | Next.jsの`notFound()`を呼び出し → 404ページを表示 |

### 12-7. S08 マイページ

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | 未ログイン | ログインプロンプト画面を表示（「ログイン / 新規登録」ボタン）。エラーではなく専用UI |
| E-02 | データ取得失敗（stamps・badges等） | エラーメッセージなし。各セクションが空表示（サイレント失敗） |

> ⚠️ E-02はサイレント失敗のため、ユーザーはデータ取得失敗に気づけない。本番前にエラー表示の追加を検討すること。

### 12-8. S09 称号一覧

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | データ取得失敗 | エラーメッセージなし。称号が0件の空リスト表示（サイレント失敗） |

> ⚠️ E-01はサイレント失敗のため、ユーザーはデータ取得失敗に気づけない。本番前にエラー表示の追加を検討すること。

> 📌 **全体的な傾向**：S04・S05・S08・S09はデータ取得失敗時にエラーメッセージを表示せずサイレント失敗する実装になっている。本番前の改善ポイントとして認識しておくこと。

### 12-9. S10 管理ページ

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | 未ログインで /admin にアクセス | `/login` にリダイレクト |
| E-02 | ログイン済みだが管理者ではない | 「このページにはアクセス権限がありません」を表示 → OKで `/map` に戻る |
| E-03 | スポット一覧の取得失敗 | 「データの取得に失敗しました」を表示 |
| E-04 | 承認待ち一覧の取得失敗 | 「データの取得に失敗しました」を表示 |

### 12-10. S11 スポット編集

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | 保存失敗 | 「保存に失敗しました。もう一度お試しください」を表示 |
| E-02 | 未保存の変更ありで戻るボタン | 「変更が保存されていません。破棄して戻りますか？」ダイアログ表示（「編集に戻る」または「破棄して戻る」） |

### 12-11. S12 スポット申請フォーム

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | 未ログインで /monuments/submit にアクセス | `/login` にリダイレクト |
| E-02 | スポット名未入力で送信 | 「スポット名を入力してください」を表示 |
| E-03 | GPS取得失敗 | 「現在地を取得できませんでした。地図上でスポットの場所をタップして指定してください」を表示 → 地図が開いてピンを置ける |
| E-04 | 送信失敗 | 「申請に失敗しました。もう一度お試しください」を表示 |
| E-05 | 入力途中で戻るボタン | 「入力内容が消えます。破棄して戻りますか？」ダイアログ表示（「入力に戻る」または「破棄して戻る」） |

### 12-12. S13 スポット新規追加

| # | 条件 | 表示メッセージ・挙動 |
|------|------|------|
| E-01 | 未ログインで /admin/monuments/new にアクセス | `/login` にリダイレクト |
| E-02 | ログイン済みだが管理者ではない | `/admin` にリダイレクト |
| E-03 | 碑の名称未入力で送信 | ブラウザ標準メッセージ。`required` 属性によりブラウザが制御。送信不可 |
| E-04 | 都道府県未選択で送信 | ブラウザ標準メッセージ。`required` 属性によりブラウザが制御。送信不可 |
| E-05 | 保存失敗 | 「保存に失敗しました。もう一度お試しください」を表示 |
| E-06 | 入力途中で戻るボタン | 「入力内容が保存されていません。破棄して戻りますか？」ダイアログ表示（「入力に戻る」または「破棄して戻る」） |
| E-07 | 未入力で戻るボタン | S10管理ページに戻る |

---

*本設計書はアプリの実装状況をもとに作成したドラフトです。実装の進捗に合わせて随時更新してください。*
